version: 1.0
runtime: nodejs22

env:
  variables:
    GOOGLE_MAP_API_KEY: "AIzaSyBkCYfaY4WVRce8IoefEvVhxoafQ7n2v6E"
    API_BASE_URL: "https://api.heilen.io"
    API_BASE_DEV_URL: "https://dev.heilen.io"

build:
  commands:
    build:
      - echo "🟢 Installing dependencies..."
      - yarn install --frozen-lockfile || yarn install
      - echo "🟢 Building Next.js app..."
      - yarn build

run:
  command: yarn start -p 8080
  network:
    port: 8080




# version: 1.0
# runtime: nodejs22

# build:
#   commands:
#     build:
#       - echo "🟢 Installing dependencies..."
#       - yarn install --frozen-lockfile || yarn install
#       - echo "🟢 Building Next.js app..."
#       - yarn build

# run:
#   command: yarn start -p 8080
#   network:
#     port: 8080
