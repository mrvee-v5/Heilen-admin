version: 1.0
runtime: nodejs22

build:
  commands:
    build:
      - echo "🟢 Checking Node version..."
      - node -v
      - echo "🟢 Installing dependencies..."
      - npm ci || npm install
      - echo "🟢 Building Next.js app..."
      - npm run build

run:
  command: npm start
  network:
    port: 8080
